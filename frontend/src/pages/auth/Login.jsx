import AuthForm from "../../components/auth/AuthForm.jsx";

const Login = () => {
    return (
        <div className="auth-page">
            <AuthForm type='login' />
        </div>
    );
};

export default Login;