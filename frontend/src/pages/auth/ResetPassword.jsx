import AuthForm from "../../components/auth/AuthForm.jsx";

const ResetPassword = () => {
    return (
        <div>
            <AuthForm type='reset' />
        </div>
    );
};

export default ResetPassword;